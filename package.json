{
  "name": "nbody-webgpu",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Real-time GPU N-body gravity simulator in raw WebGPU with tiled WGSL compute shaders",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
